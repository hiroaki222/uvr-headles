# uvr-headless: minimal separation-runtime deps (GUI deps excluded).
# `pip install -e .` installs the same deps from pyproject.toml.
# To run the GUI version (legacy/UVR.py) use legacy/requirements-gui.txt.
numpy==1.23.5
torch
librosa==0.9.2
scipy==1.9.3
audioread==3.0.0
pydub==0.25.1
onnx
onnxruntime
onnx2pytorch
einops==0.6.0
julius==0.2.7
omegaconf==2.2.3
PyYAML==6.0
ml_collections==0.1.1
resampy==0.4.2
diffq
llvmlite
pytorch_lightning==2.0.0
tqdm
soundfile>=0.12.1
